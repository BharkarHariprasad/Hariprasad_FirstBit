// To check whether given caracter is uppercase or lowercase..

void main(){
	char a = 't';
	if(a >= 'A' && a <= 'Z'){
		printf("%c is UPPERCASE", a);
	}
	else{
		printf("%c is lowercase", a);
	}
}