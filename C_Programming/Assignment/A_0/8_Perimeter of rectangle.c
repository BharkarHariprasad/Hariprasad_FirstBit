// Input the length and width of rectanagle and find their perimeter..

void main(){
	int length, width, perimeter;
	length = 15;
	width = 8;
	perimeter = 2 * (length + width);
	
	printf("Perimeter of rectangle is = %d ", perimeter);
}